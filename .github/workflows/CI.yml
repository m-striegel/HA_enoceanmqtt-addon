---
name: CI

# yamllint disable-line rule:truthy
on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  workflows:
    uses: home-assistant/addons-example/blob/main/.github/workflows/lint.yaml@main
